#!/bin/bash

	for RUN in {1..10}
	do
        DATE="$(date +'%d.%m.%y')"
	touch /opt/280323_morning/Viktar_Chumachenka/crontab_test/$RUN.$DATE.txt
    	done

# далее добавляются права (chmod 744) и прописывается путь к файлу в планировщике (crontab -e) с модификатором цикличности.

# Иван, извиняюсь за сроки сдачи, чуть хлопоты житейские отвлекли от учебного процесса. 
# Писал вам в дискорте по поводу доп.материалов по линуксу. Если не затруднит, ответьте. Как раз каникулы - больше свободного времени. 
# Спасибо!
